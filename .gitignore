node_modules
.DS_Store
dist
dist-ssr
*.local
.playwright-mcp/
build/

# Environment files (never commit secrets)
.env
.env.backup
.env.production
.env.prod
.env.staging
.env.test

# Railway setup files (local only)
.railway-setup-checklist.md

# SOPS / secrets
secrets/*.tfvars
!secrets/*.enc.tfvars

# Local Terraform variables (never commit plaintext)
infrastructure/cdktf/terraform.tfvars
